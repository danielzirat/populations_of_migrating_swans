<!DOCTYPE html>
<html lang="en">

<%- include('./partials/head.ejs') %>

<body>

<%- include('./partials/nav.ejs') %>

<div class="main-container justify-content-center">
    <div class="d-flex flex-column">
        <h3 class="text-center"> An error occured.</h3>
        <% if (locals?.code) { %>
            <h1 class="text-center"><%= code %></h1>
        <% } %>
        <p class="text-center"> <%= locals?.message ? message : 'Something went wrong.' %></p>
    </div>
</div>

<%- include('./partials/footer.ejs') %>

</body>
</html>